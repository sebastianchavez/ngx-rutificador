import { NgxRutificadorDirective } from './ngx-rutificador.directive';

describe('NgxRutificadorDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxRutificadorDirective();
    expect(directive).toBeTruthy();
  });
});
